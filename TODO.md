- [x] Push site to GitHub repo (configured git remote + initial commit + pushed main branch)
- [x] Fix Vercel static image loading (copied root images into `public/`)
- [ ] Verify build locally and (optionally) run Vercel build checks


